;-------------------------------------------------
;このキャラ独自の衣装を設定する。制服、体操服、水着は既存のものから選ぶのが楽です
;-------------------------------------------------
@CLOTHES_K53, ARGS

SELECTCASE ARGS
;キャラクター独自の下着の組み合わせを設定できます。設定必須です。一定の確率で着用します
;もしも普段は付けない、という事ならCSTR:パンツ = と等号の右側を書き込まない事で出来ます
;CFLAG:ランダム下着OFF = 1とした場合には、ここで設定した下着のみを身に着けるようになります
CASE "内衣"
	CSTR:胖次 = 蕾丝的胖次/Ｈ的颜色/
	CSTR:胸罩 = 蕾丝胸罩/Ｈ的颜色/

;衣装に関わらず常に付けているアクセサリです。設定しなくてもかまいません
CASE "飾品"
	CALL SET_OTHER, ""

;学園での普段着です。設定必須です。
;セーラー服, ミニスカセーラー服, ブレザー, ミニスカブレザー, ベスト,アメスクなど
;黒いセーラー服を着せたい場合には CALL SET_COSTUME, "セーラー服/黒" のように書き込むことで色を変えることが出来ます
;(RESOURCES→MAIDFACE→13服 フォルダ内にある MAIDFACE13.csv の該当衣装を見て色を選ばないとならないため上級者向きです)
;また、ブラウス等の上にサスペンダーを着けたい場合には CALL SET_OTHER, "サスペンダー" として下さい
CASE "制服"
	CALL SET_COSTUME, "水手服/灰色"
;学園の体育の授業で着ます。この格好のまま部室に現れたりもする。設定必須です
;体操服ブルマ, 体操服ハイレグブルマ, 体操服ハーフパンツ, 体操服スパッツ, 体操服ジャージなど
CASE "体操服"
	IF COND("秋") + COND("冬")
		CALL SET_COSTUME, "体操服运动衫/黒"
	ELSE
		CALL SET_COSTUME, "体操服布鲁马/黒"
	ENDIF
;学園の水泳の授業で着ます。この格好のまま部室に現れたりもする。設定必須です
;スク水, 競泳水着, 白ビキニ, 黒ビキニなど
CASE "泳装"
	CALL SET_COSTUME, "競泳泳装/黒"

;自宅など学園外で着ます。設定必須です
CASE "便装"
	CALL SET_SHIRT, "蝴蝶結女式衬衫"
	CALL SET_SKIRT, "裙"
	CALL SET_OUTER, "连褲襪"
	CALL SET_SHOES, "小皮靴"

;自分の部屋(notあなたの部屋)で着ます
CASE "居家服"
	IF COND("秋") + COND("冬")
		CALL SET_SHIRT, "背心/灰色"
	ELSE
		CALL SET_SHIRT, "无袖女式衬衫"
	ENDIF
	CALL SET_SKIRT, "裙"
;デート以外で家から外に出る際に着ます。ラブホ呼出とかね
CASE "外出着"
	IF COND("秋") + COND("冬")
		CALL SET_SHIRT, "蝴蝶結女式衬衫"
	ELSE
		CALL SET_SHIRT, "无袖女式衬衫"
	ENDIF
	CALL SET_SKIRT, "裙"
	CALL SET_OUTER, "连褲襪"
	CALL SET_SHOES, "小皮靴"
	SIF COND("冬")
		CALL SET_OTHER, "粗呢外套"

;もちろんデートの際に着ます
CASE "约会衣装"
	IF COND("秋") + COND("冬")
		CALL SET_SHIRT, "褶边女式衬衫/黒"
		CALL SET_SKIRT, "裙"
		CALL SET_SOCKS, "连褲襪"
		CALL SET_SHOES, "靴"
		SIF COND("冬")
			CALL SET_OTHER, "粗呢外套"
	ELSE
		CALL SET_SHIRT, "无袖女式衬衫"
		CALL SET_SKIRT, "迷你裙"
		CALL SET_SOCKS, "及膝襪"
		CALL SET_SHOES, "小皮靴"
	ENDIF
;朝Ｈの際に着ます
CASE "朝袭衣装"
	IF COND("同居中")
		CALL SET_SHIRT, "宽松女式衬衫"
	ELSE
		CALL SET_COSTUME, "水手服/灰色"
	ENDIF
;夜這いＨの際に着ます
CASE "夜袭衣装"
	CALL SET_SHIRT, "宽松女式衬衫"
ENDSELECT


;-------------------------------------------------
;時間帯や場所に応じた髪型、表情の変化を記述する。設定しなくてもかまいません
;CALL SETFLAG, "普段の髪型復元", TARGET で変化させた髪型などを普段のものに戻すことが出来る
;-------------------------------------------------
@MAIDFACE_K53
IF COND("约会中")
	
ELSEIF PLACE("自宅")
	
ELSE
	
ENDIF
