;-------------------------------------------------
;このキャラ独自の衣装を設定する。制服、体操服、水着は既存のものから選ぶのが楽です
;-------------------------------------------------
@CLOTHES_K47, ARGS

SELECTCASE ARGS
;高めの頻度で着用します。設定しなくてもかまいません
CASE "内衣"
	CALL SET_PANTIES, "條紋胖次"
	CALL SET_BRA, "運動胸罩"

;衣装に関わらず常に付けているアクセサリです。設定しなくてもかまいません
CASE "飾品"
	CALL SET_OTHER, "赤い鈴付き项圈"

;学園での普段着です。設定必須です。
;セーラー服, ミニスカセーラー服, ブレザー, ミニスカブレザー, アメスクなど
CASE "制服"
	CALL SET_COSTUME, "旗袍"
;学園の体育の授業で着ます。この格好のまま部室に現れたりもする。設定必須です
;体操服ブルマ, 体操服ハーフパンツ, 体操服スパッツ, 体操服ジャージなど
CASE "体操服"
	CALL SET_COSTUME, "旗袍"
;学園の水泳の授業で着ます。この格好のまま部室に現れたりもする。設定必須です
;スク水, 競泳水着, 白ビキニ, 黒ビキニなど
CASE "泳装"
	CALL SET_COSTUME, "旗袍"

;自宅など学園外で着ます。設定必須です
CASE "便装"
	CALL SET_COSTUME, "旗袍"

;自分の部屋(notあなたの部屋)で着ます
CASE "居家服"

;デート以外で家から外に出る際に着ます。ラブホ呼出とかね
CASE "外出着"
	CALL SET_COSTUME, "旗袍"
;もちろんデートの際に着ます
CASE "约会衣装"
	CALL SET_COSTUME, "旗袍"
ENDSELECT
SIF COND("角色メイク中")
    CALL SETFLAG, "衣装全脱ぎ"

;-------------------------------------------------
;時間帯や場所に応じた髪型、表情の変化を記述する。設定しなくてもかまいません
;-------------------------------------------------
@MAIDFACE_K47
IF PLACE("自宅") && ABL:信頼 >= 3
	CSTR:髪長 = 長
	CSTR:後髪 = 自然
	CSTR:改髪 = 无特殊髪型
;シャワー後もこの姿にしたかったけど私には無理だった
ELSEIF CFLAG:淋浴済
	CSTR:髪長 = 長
	CSTR:後髪 = 自然
	CSTR:改髪 = 无特殊髪型
ELSE
	CALL SETFLAG, "普段の髪型復元", TARGET
ENDIF
SIF ABL:信頼 >= 3
	CSTR:嘴形 = ω
