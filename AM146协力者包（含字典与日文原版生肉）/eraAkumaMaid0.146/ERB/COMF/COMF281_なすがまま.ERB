@COM281
;なすがまま
;ぶっちゃけ、絶対に脱出しない”もがく”です
IF STATE("恍惚", PLAYER) && FLAG:恍惚状態 == 1
	CALL SHOW_STATUS
	FLAG:COM_ABLEの一括OFF = 0
	SIF PREVCOM >= 0
		CALL SHOW_PREVCOM
ELSE
	PRINTL なすがまま
ENDIF
STR:前回コマンド = なすがまま
CALL KOJO_MESSAGE_COM
PRINTL 

SIF REFUSE_CHECK()
	RETURN 0

;FIRSTCOMフラグON
CALL SET_CHOSECOM, TARGET, 281

CALL SET_COUNTER_ABL

SIF TEQUIP:Ｗ押し倒し
	CALL SET_SELECTCOM_MULTI, "Ｗ押し倒し"

TRYJUMPFORM COM{SELECTCOM}

RETURN 1
